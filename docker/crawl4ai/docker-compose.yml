services:
  crawl4ai:
    image: unclecode/crawl4ai:0.8.6
    container_name: crawl4ai
    restart: unless-stopped
    shm_size: "1g"
    env_file:
      - .env
    volumes:
      - /opt/appdata/crawl4ai:/root/.crawl4ai
    ports:
      - "127.0.0.1:11235:11235"
    networks:
      - forge-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11235/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  forge-net:
    external: true
