services:
  reranker:
    build: .
    container_name: reranker
    environment:
      - RERANKER_MODEL=ms-marco-MiniLM-L-12-v2
    ports:
      - "127.0.0.1:8787:8787"
    networks:
      - forge-net
    restart: unless-stopped

networks:
  forge-net:
    external: true
