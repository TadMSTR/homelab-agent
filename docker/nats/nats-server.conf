# NATS Server

port: 4222

# HTTP monitoring dashboard (read-only, proxied via SWAG)
http_port: 8222

# JetStream
jetstream {
  store_dir: /data
  max_memory_store: 256MB
  max_file_store: 2GB
}

# Logging
logtime: true
log_size_limit: 100MB
