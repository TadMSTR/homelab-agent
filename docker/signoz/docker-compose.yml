services:
  signoz-init-clickhouse:
    image: clickhouse/clickhouse-server:25.5.6
    container_name: signoz-init-clickhouse
    restart: on-failure
    command:
      - bash
      - -c
      - |
        # Binary was staged at initial deploy — skip re-download if already present (M2 fix 2026-04-15)
        if [ -f /var/lib/clickhouse/user_scripts/histogramQuantile ]; then
          echo "histogramQuantile already present, skipping download"
          exit 0
        fi
        version="v0.0.1"
        node_os=$$(uname -s | tr '[:upper:]' '[:lower:]')
        node_arch=$$(uname -m | sed s/aarch64/arm64/ | sed s/x86_64/amd64/)
        echo "Fetching histogram-binary for $${node_os}/$${node_arch}"
        cd /tmp
        wget -O histogram-quantile.tar.gz "https://github.com/SigNoz/signoz/releases/download/histogram-quantile%2F$${version}/histogram-quantile_$${node_os}_$${node_arch}.tar.gz"
        tar -xvzf histogram-quantile.tar.gz
        mv histogram-quantile /var/lib/clickhouse/user_scripts/histogramQuantile
    volumes:
      - /opt/appdata/signoz/clickhouse-user-scripts:/var/lib/clickhouse/user_scripts/
    networks:
      - signoz-internal

  signoz-zookeeper:
    image: signoz/zookeeper:3.7.1
    container_name: signoz-zookeeper
    restart: unless-stopped
    user: root
    environment:
      - ZOO_SERVER_ID=1
      - ALLOW_ANONYMOUS_LOGIN=yes
      - ZOO_AUTOPURGE_INTERVAL=1
    volumes:
      - /opt/appdata/signoz/zookeeper:/bitnami/zookeeper
    healthcheck:
      test: ["CMD-SHELL", "curl -s -m 2 http://localhost:8080/commands/ruok | grep error | grep null"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - signoz-internal

  signoz-clickhouse:
    image: clickhouse/clickhouse-server:25.5.6
    container_name: signoz-clickhouse
    hostname: clickhouse
    restart: unless-stopped
    depends_on:
      - signoz-init-clickhouse
      - signoz-zookeeper
    environment:
      - CLICKHOUSE_SKIP_USER_SETUP=1
    volumes:
      - /opt/appdata/signoz/clickhouse:/var/lib/clickhouse/
      - /opt/appdata/signoz/clickhouse-user-scripts:/var/lib/clickhouse/user_scripts/
      - /opt/appdata/signoz/config.xml:/etc/clickhouse-server/config.xml
      - /opt/appdata/signoz/users.xml:/etc/clickhouse-server/users.xml
      - /opt/appdata/signoz/custom-function.xml:/etc/clickhouse-server/custom-function.xml
      - /opt/appdata/signoz/cluster.xml:/etc/clickhouse-server/config.d/cluster.xml
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "0.0.0.0:8123/ping"]
      interval: 30s
      timeout: 5s
      retries: 3
    ulimits:
      nproc: 65535
      nofile:
        soft: 262144
        hard: 262144
    networks:
      - signoz-internal
      - grafana-datasources

  signoz-migrator:
    image: signoz/signoz-otel-collector:v0.144.2
    container_name: signoz-migrator
    restart: on-failure
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
    environment:
      - SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_DSN=tcp://default:${SIGNOZ_CLICKHOUSE_PASSWORD}@clickhouse:9000
      - SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_CLUSTER=cluster
      - SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_REPLICATION=true
      - SIGNOZ_OTEL_COLLECTOR_TIMEOUT=10m
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        /signoz-otel-collector migrate bootstrap &&
        /signoz-otel-collector migrate sync up &&
        /signoz-otel-collector migrate async up
    networks:
      - signoz-internal

  signoz:
    image: signoz/signoz:v0.118.0
    container_name: signoz
    restart: unless-stopped
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
    environment:
      - SIGNOZ_ALERTMANAGER_PROVIDER=signoz
      - SIGNOZ_TELEMETRYSTORE_CLICKHOUSE_DSN=tcp://default:${SIGNOZ_CLICKHOUSE_PASSWORD}@clickhouse:9000
      - SIGNOZ_SQLSTORE_SQLITE_PATH=/var/lib/signoz/signoz.db
      - SIGNOZ_TOKENIZER_JWT_SECRET=${SIGNOZ_JWT_SECRET}
    volumes:
      - /opt/appdata/signoz/sqlite:/var/lib/signoz/
    ports:
      - "127.0.0.1:8080:8080"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "localhost:8080/api/v1/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - signoz-internal
      - forge-net

  signoz-otel-collector:
    image: signoz/signoz-otel-collector:v0.144.2
    container_name: signoz-otel-collector
    restart: unless-stopped
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
    environment:
      - LOW_CARDINAL_EXCEPTION_GROUPING=false
      - SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_DSN=tcp://default:${SIGNOZ_CLICKHOUSE_PASSWORD}@clickhouse:9000
      - SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_CLUSTER=cluster
      - SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_REPLICATION=true
      - SIGNOZ_OTEL_COLLECTOR_TIMEOUT=10m
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        /signoz-otel-collector migrate sync check &&
        /signoz-otel-collector --config=/etc/otel-collector-config.yaml --manager-config=/etc/manager-config.yaml --copy-path=/var/tmp/collector-config.yaml
    volumes:
      - /opt/appdata/signoz/otel-collector-config.yaml:/etc/otel-collector-config.yaml
      - /opt/appdata/signoz/otel-collector-opamp-config.yaml:/etc/manager-config.yaml
    ports:
      - "4317:4317"   # OTLP gRPC — LAN-accessible for claudebox → forge telemetry
      - "4318:4318"   # OTLP HTTP
    networks:
      - signoz-internal
      - forge-net

networks:
  signoz-internal:
    name: signoz-internal
  forge-net:
    external: true
  grafana-datasources:
    external: true
