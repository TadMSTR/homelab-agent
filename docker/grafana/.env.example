# InfluxDB
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=changeme  # Generate: openssl rand -hex 16
INFLUXDB_ORG=homelab
INFLUXDB_BUCKET=metrics
INFLUXDB_ADMIN_TOKEN=changeme  # Generate: openssl rand -hex 32

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme  # Generate: openssl rand -hex 16
