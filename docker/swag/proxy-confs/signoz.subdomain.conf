server {
    listen 443 ssl;
    server_name signoz.*;

    include /config/nginx/ssl.conf;
    include /config/nginx/proxy.conf;

    location / {
        include /config/nginx/proxy.conf;
        proxy_pass http://signoz:8080;
    }
}
